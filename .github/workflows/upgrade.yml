# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

name: upgrade
on:
  workflow_dispatch: {}
  schedule:
    - cron: 0 9 * * 1
jobs:
  upgrade:
    name: Upgrade
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      patch_created: ${{ steps.create_patch.outputs.patch_created }}
    env:
      NODE_AUTH_TOKEN: ${{secrets.GPR_NPM_INSTALL_TOKEN}}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          always-auth: true
          registry-url: https://npm.pkg.github.com/
          node-version: '24'
          scope: "@dpc-sdp"
      - name: Install dependencies
        run: npm ci
      - name: Update projenrc
        run: |
          npm run update-projenrc
      - name: Run projen upgrade
        run: npx projen upgrade
      - name: Find mutations
        id: create_patch
        run: |-
          git add .
          git diff --staged --patch --exit-code > repo.patch || echo "patch_created=true" >> $GITHUB_OUTPUT
        working-directory: ./
      - name: Upload patch
        if: steps.create_patch.outputs.patch_created
        uses: actions/upload-artifact@v4.4.0
        with:
          name: repo.patch
          path: repo.patch
          overwrite: true
  pr:
    name: Create Pull Request
    needs: upgrade
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    if: ${{ needs.upgrade.outputs.patch_created }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Download patch
        uses: actions/download-artifact@v4
        with:
          name: repo.patch
          path: ${{ runner.temp }}
      - name: Apply patch
        run: '[ -s ${{ runner.temp }}/repo.patch ] && git apply ${{ runner.temp }}/repo.patch || echo "Empty patch. Skipping."'
      - name: Set git identity
        run: |-
          git config user.name "sdpdeploy"
          git config user.email "sdp.devs@dpc.vic.gov.au"
      - name: Create Pull Request
        id: create-pr
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.PROJEN_GITHUB_TOKEN }}
          commit-message: |-

            chore(deps): upgrade dependencies

            Upgrades SDP Ripple project dependencies. See details in [workflow run].

            [Workflow Run]: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

            ------

            *Automatically created by projen via the "upgrade" workflow - See .projenrc.ts *
          branch: "release/build-${{ github.run_number }}"
          title: "chore(deps): upgrade dependencies"
          body: |-

            Upgrades project dependencies. See details in [workflow run].

            [Workflow Run]: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

            ------

            *Automatically created by projen via the "upgrade" workflow*
          author: sdpdeploy <sdp.devs@dpc.vic.gov.au>
          committer: sdpdeploy <sdp.devs@dpc.vic.gov.au>
          signoff: true
      - name: Enable Pull Request Automerge
        if: steps.create-pr.outputs.pull-request-operation == 'created'
        uses: peter-evans/enable-pull-request-automerge@v3
        with:
          token: ${{secrets.PROJEN_GITHUB_TOKEN}}
          pull-request-number: ${{ steps.create-pr.outputs.pull-request-number }}
          merge-method: squash
